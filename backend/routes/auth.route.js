import { Router } from "express";
const router = Router()

// login

router.get("/", (req,res)=>{
    res.status(200).send("comments coming soon...")
})

// register



// forgot password

export default router
