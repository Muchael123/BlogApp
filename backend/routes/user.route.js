import { Router } from "express";
const router = Router()

//get all posts using paging
router.get("/", (req,res)=>{
    res.status(200).send("coming soon...")
})
//login

//singup

//reset password

export default router